apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: ruberoo
type: Opaque
# NOTE: Do NOT commit real secrets to Git. The real secret should be created in-cluster using
# kubectl create secret generic jwt-secret --from-literal=secret-key='<YOUR_SECRET>'
# This file is a template and intentionally contains no secret value.
stringData:
  # Placeholder; replace locally instead of committing secrets to Git
  secret-key: "<CREATE_SECRET_IN_CLUSTER>"
